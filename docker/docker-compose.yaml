
name: taskio-redis

services:
  redis:
    image: redis:7.2-alpine
    container_name: taskio-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --requirepass 123456

volumes:
  redis_data: